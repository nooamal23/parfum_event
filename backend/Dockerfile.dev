# Dockerfile de DÉVELOPPEMENT uniquement (hot-reload via nodemon).
# La production utilise Dockerfile (multi-stage, sans dépendances dev, utilisateur non-root).
FROM node:20-alpine
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY src ./src
EXPOSE 4000
CMD ["npx", "nodemon", "src/server.js"]